query CreditFacilityBalance($journalId: UUID!, $facilityId: UUID!) {
  facility: balance(
    journalId: $journalId
    accountId: $facilityId
    currency: "USD"
  ) {
    settled {
      normalBalance {
        units
        currency
      }
    }
  }
}
