query CreditFacilityBalance(
  $journalId: UUID!
  $collateralId: UUID!
  $facilityId: UUID!
  $disbursedReceivableId: UUID!
  $interestReceivableId: UUID!
) {
  collateral: balance(
    journalId: $journalId
    accountId: $collateralId
    currency: "BTC"
  ) {
    settled {
      normalBalance {
        units
        currency
      }
    }
  }
  facility: balance(
    journalId: $journalId
    accountId: $facilityId
    currency: "USD"
  ) {
    settled {
      normalBalance {
        units
        currency
      }
    }
  }
  disbursedReceivable: balance(
    journalId: $journalId
    accountId: $disbursedReceivableId
    currency: "USD"
  ) {
    settled {
      normalBalance {
        units
        currency
      }
    }
  }
  interestReceivable: balance(
    journalId: $journalId
    accountId: $interestReceivableId
    currency: "USD"
  ) {
    settled {
      normalBalance {
        units
        currency
      }
    }
  }
}
