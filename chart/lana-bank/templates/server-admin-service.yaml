apiVersion: v1
kind: Service
metadata:
  name: {{ template "lanaBank.admin.fullname" . }}
  labels:
    app: {{ template "lanaBank.admin.fullname" . }}
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    release: "{{ .Release.Name }}"
  {{ with .Values.lanaBank.server.admin.annotations }}
  annotations:
    {{- toYaml . | nindent 4 -}}
  {{ end }}
spec:
  type: {{ .Values.lanaBank.server.admin.type }}
  {{ if and (eq .Values.lanaBank.server.admin.type "LoadBalancer") .Values.lanaBank.server.admin.staticIP }}
  loadBalancerIP: {{ .Values.lanaBank.server.admin.staticIP }}
  {{ end }}
  ports:
  - port: {{ .Values.lanaBank.server.admin.port }}
    targetPort: {{ .Values.lanaBank.server.admin.port }}
    protocol: TCP
    name: http
  selector:
    app: {{ template "lanaBank.server.fullname" . }}
