query TermsTemplateGet($id: UUID!) {
  termsTemplate(id: $id) {
    termsId
    name
    values {
      annualRate
      accrualInterval
      duration {
        period
        units
      }
      liquidationCvl
      marginCallCvl
      initialCvl
    }
  }
}
