config {
	type: "table",
	description: "Each row represents a unique customer.",
	assertions: {
		uniqueKey: ["customer_id"],
		nonNull: ["customer_id"],
	},
}

SELECT *

FROM ${ref("int_customers")}
