config {
	type: "table",
	description: "Each row represents a unique account.",
	assertions: {
		uniqueKey: ["account_id"],
		nonNull: ["account_id", "normal_balance_type", "is_account_set"],
		rowConditions: [
			"normal_balance_type IN ('debit', 'credit')"
		],
	},
}

SELECT *

FROM ${ref("int_accounts")}
