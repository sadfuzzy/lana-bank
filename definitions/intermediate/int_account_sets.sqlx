config {
	type: "table",
	assertions: {
		uniqueKey: ["set_id"],
	},
}

SELECT
	id AS set_id,
	JSON_VALUE(ANY_VALUE(values HAVING MAX recorded_at), "$.name") AS set_name,

FROM ${ref({
	name: "account_sets",
	schema: envs.currentImportSchema
})}

GROUP BY id
