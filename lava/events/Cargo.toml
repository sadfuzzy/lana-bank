[package]
name = "lava-events"
version = "0.2.9-dev"
edition = "2021"

[features]

fail-on-warnings = []

[dependencies]
governance = { path = "../../core/governance" }
core-user = { path = "../../core/user" }
outbox = { path = "../../lib/outbox" }

serde = { workspace = true }
